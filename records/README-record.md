# Record: README

What happened to [README](../README.md) after it was written: its review history, kept out of the
document itself.

## Cold review

Reviewed on 2026-09-25 by cold-reviewer. Saved unchanged; what was folded in is logged under Changes since the review.

The review ran through `hooks/run-agent.sh`; its first run ended in an API timeout after 22 turns,
and this is the reply of the resumed session.

| # | Kind | Where | Finding | Affects | Evidence | What would settle it |
|---|---|---|---|---|---|---|
| 1 | STALE | Requirements: "spike sandboxing needs a version that honours `sandbox.*` settings passed with `--settings`" and "Spikes haven't been run anywhere else" | The README says only spikes depend on the sandbox and on macOS. That is out of date. Every agent that `/research`, `/spec` and `/cold-review` launch now runs under `--settings hooks/agent-sandbox.json`. Its exclusion list is built around a macOS problem: Go programs such as `gh` fail secure connections (TLS) under the macOS sandbox. So a reader on an older Claude Code, or on Linux, will think the agents are sandboxed when they may not be. | correctness | `hooks/run-agent.sh:68-71`; `hooks/agent-sandbox.json:3-5`; `BASELINE.md`, "Agents run headless in an OS sandbox" (the Go TLS failure) | Move the version and macOS requirement from "spikes" to "all agents". Then, on the version the README names, check that an agent's attempt to read `~/.aws` is refused. |
| 2 | ASSUMPTION | Layout: "inside Claude Code's OS sandbox (no credential reads, no secret environment variables, … Bash network only to an allowlist)" | This is stated as always true, but it isn't. `gh`, `repo-health.sh`, `gcp-skus.sh` and `reddit-search.sh` are listed as excluded commands, so they run outside the sandbox. They have full network access, and `gh` reads its own token. The researcher also keeps its MCP servers (outside tools such as the AWS and Terraform docs). For these, only the guard's checks on arguments apply. | correctness | `hooks/agent-sandbox.json:5`; `hooks/run-agent.sh:48-50`; `run-agent.sh:18-19` names these gaps, but the README doesn't | Name the exclusions and the researcher's MCP access in the README. |
| 3 | GAP | "Notes live in `~/notes`" and Install | The README never says what `~/notes` needs, and this repo doesn't supply it. It must be a git repo, because the skills commit to it. It must contain `templates/idea.md`, `templates/research.md`, `templates/research-ideas.md` and `CLAUDE.md`. At `ideas` depth it also needs `projects/mindshare/attention-evidence.md`. Every agent run adds `~/notes` as a working directory. A reader who follows Install alone gets `/idea` and `/research` failing. | correctness | `skills/idea/SKILL.md:16,23`; `agents/researcher.md:37`; `skills/research/SKILL.md:125,131`; `hooks/run-agent.sh:69` (`--add-dir "$HOME/notes"`); `tests/agent-evals/run.sh:49` | Add a prerequisite step, or ship the templates. A run with no `~/notes` would show exactly how it fails. |
| 4 | ASSUMPTION | "`/spec <research note>` writes an implementation spec into the repo the change touches" | `/spec` only works on a repo under `~/code`. Anywhere else it asks for a repo path and stops. Its pre-approved edits cover only `~/code/**`. The Install paths hint at this layout, but the README never says it is required. | correctness | `skills/spec/SKILL.md:51` ("It must be a repo under `~/code`"); `skills/spec/SKILL.md:5` (`Edit(~/code/**)`, `mkdir -p ~/code/*`) | State the `~/code` requirement in the README. |
| 5 | WRONG / COST | "the reviewer edits nothing and neither does the skill" | The skill does write files. It saves reviews to `records/<basename>-record.md`, beside the document, in the document's repo. It edits the document when the user picks findings to fold in. For a spec, it saves the delta review without asking. So a README or runbook that gets reviewed gains a new `records/` folder the reader didn't expect. | correctness | `skills/cold-review/SKILL.md:24-28,176-194` ("For a spec, save it without asking"); `SKILL.md:5` (`Edit(~/code/**)`, `Edit(~/notes/**)`) | Say "edits nothing unless asked; saves the review to a record file beside the document". |
| 6 | STALE | "Recent full runs of its five cases cost $1.22 and $1.82" | There have been six committed cases since 2026-09-24, and the last four runs cost $0.73, $0.82, $1.06 and $1.18. The limit per case is $5 by default, and the cases run in parallel, so the worst case is $30. The README doesn't mention that ceiling. | neither: the real costs are at or below what the README says, so no one is misled into overspending | `tests/agent-evals/cases/` (6 committed, plus 2 untracked); `BASELINE.md` sixth to ninth runs; `run.sh:31` (`EVAL_MAX_USD:-5`) | Update the figures and mention `EVAL_MAX_USD`. |
| 7 | STALE | "`docs/specs/2026-09-17-spec-spike-phase.md` is a worked example of the output" | The example uses the older layout. Its review sits inside the spec at `## Cold review`, with no `records/` file. It is 7,414 words, against the 4,000-word limit. It cites `agents/spec-reviewer.md`, which no longer exists. All of this contradicts step 3's description of the record. | neither: the skills still read the older layout (`check-spec.py:18-20`, with a WARN), and the spec is `status: done`, so the length check only warns | `docs/specs/2026-09-17-spec-spike-phase.md:4,50,313`; `wc -w` = 7414; no `docs/specs/records/` | Say it predates the record layout, or point to a newer example. |
| 8 | STALE | Layout: "the PreToolUse guard those subagents' Bash, Read, Grep, Glob and Write calls go through" | The agents are no longer subagents: they run as headless sessions. WebFetch also goes through the guard, in `fetch` mode. This also makes the contrast "The spiker isn't a subagent" lose its point. | neither: the wording is out of date, but it doesn't change what anyone does | `agents/research-verifier.md:17-18`; `agents/researcher.md:17-18`; `hooks/agent-guard.py:46,52` | Reword. |
| 9 | COLD-READ | Checks: `replay_guard.py  # guard vs real commands and reads` | The script only works on the author's machine. It replays transcripts from `projects/-Users-chrisadkin/*/subagents/…` against base commit `c7adaf4`. Its reads half only picks up `subagents/agent-*.jsonl`, which headless `run-agent.sh` sessions don't produce, so new runs no longer add to it. Someone who copies the repo ("copy what's useful") has nothing for it to replay. | neither: it is a check for the author's own use, not an entry point for other readers | `tests/replay_guard.py:4,30-31,127,178` | Run it on a machine without those transcripts and see what it prints. |
| 10 | GAP | Requirements: "Tested on 2.1.274"; `tests/` line | Nothing in the repo records the 2.1.274 version. The `tests/` description leaves out `test_prepare_spike.py`, and the untracked `tests/skill-evals/`, `test_run_agent.py` and two new eval cases. | neither: the version is a claim about testing, and the untracked files aren't committed yet | `grep` finds no `2.1.` in the repo; `git status` shows `?? tests/skill-evals/` and `?? tests/test_run_agent.py` | Check `claude --version` against a BASELINE run. |

Counts: 10 findings - 5 correctness, 0 requirement, 5 neither
Neither: 6, 7, 8, 9, 10
Cold read: no - "Notes live in `~/notes`": nothing says what has to already be in `~/notes`, or that it must be a git repo
Needs a run: 1, 3, 9, 10

## Changes since the review

- Not reviewed: Requirements says every agent and spike needs `sandbox.*` settings via `--settings` (2.1.219 or later, last run on 2.1.282), and that the macOS sandbox shapes all of them, from README review row 1, on 2026-09-25.
- Not reviewed: Layout names what runs outside the sandbox (`gh`, the three research scripts, the Read, WebFetch and WebSearch tools, the researcher's MCP servers), from README review row 2, on 2026-09-25.
- Not reviewed: a new section, Set up ~/notes, lists what `~/notes` must hold, from README review row 3, on 2026-09-25.
- Not reviewed: step 3 says `/spec` needs a git repo under `~/code`, from README review row 4, on 2026-09-25.
- Not reviewed: `/cold-review`'s description says the skill edits for picked findings and saves reviews to a record, from README review row 5, on 2026-09-25.
- Not reviewed: the README was restructured for readability: a plain summary and a short glossary open it; step 3 of the flow is split into checks and the record; `/cold-review` has its own section; Requirements, Install and Set up ~/notes come before Layout; the sandbox and guard detail moved out of Layout into a new section, How the agents are contained, which now names every tool the guard covers (adding WebFetch and Edit, from README review row 8's evidence); costs are gathered into What things cost, on 2026-09-25.
- Not reviewed: How the agents are contained says no hook checks WebSearch or the researcher's MCP tools, and names which agents are granted them, from the README delta review (2026-09-25) row 1, on 2026-09-25.
- Not reviewed: What things cost gives the agent evaluations' latest full run ($4.02, ten cases) and their $5-per-case cap, from the README delta review (2026-09-25) row 2, on 2026-09-25.
- Not reviewed: Requirements drops the unsourced 2.1.187 figure and says no older version has been run; 2.1.282 checked with `claude --version`, from the README delta review (2026-09-25) row 3, on 2026-09-25.
- Not reviewed: Set up ~/notes says `/spec` needs `~/notes` and `/cold-review` doesn't, and that a missing `--add-dir` directory doesn't stop a run (checked on 2.1.282), from the README delta review (2026-09-25) row 4, on 2026-09-25.
- Not reviewed: Layout adds `hooks/git-read.py` and lists every test file, from the README delta review (2026-09-25) row 5, on 2026-09-25.
- Not reviewed: What things cost and Checks add the skill evaluations, their cost and command, from the README delta review (2026-09-25) row 6, on 2026-09-25.
- Not reviewed: a workflow diagram, `docs/workflow.png` (source `docs/workflow.svg`), added after the opening paragraphs, with alt text describing it, on 2026-09-25.
- Not reviewed: the workflow diagram is now drawn by `docs/diagram/workflow.py` in a wide and a narrow layout, each light and dark; the README shows the narrow one, switching to dark with the viewer's theme, and links the full-size ones; arrowheads are drawn along their lines, on 2026-09-25.
